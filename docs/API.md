# LogApart API reference

<!-- Generated by backend/scripts/api-reference.js. Do not edit by hand: run `npm run docs:api`. -->

153 routes. Every path except `/api/auth` and the health checks needs a
`Bearer` token and a password that is no longer the one-time one. Responses are
`{ success, data?, message? }`; a refused body comes back `400` with `code:
INVALID_REQUEST` and every problem in `errors`.

- **Who:** roles the route admits. `Admin` includes every administrative role. A
  controller can narrow this further, for example to the home of the caller.
- **Body:** fields the validator checks. `?` marks an optional field.
- **(limited):** the tighter rate limit applies on top of the standard 300 a minute.

## Health

| Method | Path | Who | Body |
| :--- | :--- | :--- | :--- |
| GET | `/api/health` | Anyone | |
| GET | `/api/health/ready` | Anyone | |

## `/api/auth`

| Method | Path | Who | Body |
| :--- | :--- | :--- | :--- |
| POST | `/api/auth/login` | Anyone | `email` (string), `password` (string) |
| GET | `/api/auth/me` | Any signed-in user |  |
| POST | `/api/auth/logout` | Any signed-in user |  |
| POST | `/api/auth/change-password` | Any signed-in user | `current_password` (string), `new_password` (string) |

## `/api/units`

| Method | Path | Who | Body |
| :--- | :--- | :--- | :--- |
| GET | `/api/units` | Admin, Security |  |
| POST | `/api/units/assign` | Admin | `unit_id` (integer), `name` (string), `email` (string), `phone`? (string), `type`? (OWNER / TENANT), `move_in_date`? (date), `emergency_contact`? (string), `area`? (number) |
| PUT | `/api/units/:unit_id/resident` | Admin | `name`? (string), `phone`? (string), `emergency_contact`? (string), `type`? (OWNER / TENANT), `area`? (number) |
| GET | `/api/units/:unit_id/dues` | Admin |  |
| POST | `/api/units/vacate` | Admin | `unit_id` (integer), `move_out_date`? (date), `waive_dues`? (boolean), `waiver_reason`? (string) |
| GET | `/api/units/move-outs` | Admin |  |
| GET | `/api/units/:unit_id/move-out` | Admin |  |
| POST | `/api/units/:unit_id/move-out` | Admin | `notice_given_on`? (date), `planned_move_out` (date), `note`? (string) |
| POST | `/api/units/:unit_id/move-out/release` | Admin | `step`? (HELPERS_UNLINKED / PARKING_RELEASED / PASSES_CANCELLED) |
| POST | `/api/units/:unit_id/move-out/cancel` | Admin |  |
| POST | `/api/units/:unit_id/resident/password` | Admin | `reason` (string) |

## `/api/tickets`

| Method | Path | Who | Body |
| :--- | :--- | :--- | :--- |
| GET | `/api/tickets` | Admin |  |
| GET | `/api/tickets/sla/breaches` | Admin |  |
| POST | `/api/tickets/sla/escalate` | Admin |  |
| POST | `/api/tickets` | Admin | `title` (string), `description` (string), `category`? (PLUMBING / ELECTRICAL / STRUCTURAL / LIFT / COMMON / OTHER), `scope`? (UNIT / COMMON), `location`? (string), `unit_id`? (integer), `asset_id`? (integer), `priority`? (LOW / MEDIUM / HIGH / URGENT) |
| PUT | `/api/tickets/:id` | Admin | `status`? (OPEN / IN_PROGRESS / RESOLVED / CLOSED), `priority`? (LOW / MEDIUM / HIGH / URGENT), `assigned_to_id`? (integer), `asset_id`? (integer) |
| GET | `/api/tickets/:id/comments` | Admin, Resident |  |
| POST | `/api/tickets/:id/comments` | Admin, Resident | `body` (string) |
| POST | `/api/tickets/:id/rating` | Resident | `rating` (integer), `note`? (string) |
| POST | `/api/tickets/:id/reopen` | Resident | `reason` (string) |

## `/api/security`

| Method | Path | Who | Body |
| :--- | :--- | :--- | :--- |
| GET | `/api/security/visitors` | Admin, Security |  |
| GET | `/api/security/passes/lookup` | Security |  |
| PUT | `/api/security/passes/:id/admit` | Security | `vehicle_number`? (string), `vehicle_type`? (string) |
| POST | `/api/security/visitors` | Security | `visitor_name` (string), `unit_id` (integer), `visitor_phone`? (string), `vehicle_number`? (string) |
| PUT | `/api/security/visitors/:id` | Security | `visitor_name`? (string), `visitor_phone`? (string), `unit_id`? (integer), `purpose`? (GUEST / DELIVERY / SERVICE / MAID / OTHER), `company`? (string), `vehicle_number`? (string), `vehicle_type`? (string) |
| PUT | `/api/security/visitors/:id/checkout` | Security |  |
| DELETE | `/api/security/visitors/:id` | Security | `reason` (string) |

## `/api/dashboard`

| Method | Path | Who | Body |
| :--- | :--- | :--- | :--- |
| GET | `/api/dashboard/stats` | Admin |  |

## `/api/notifications`

| Method | Path | Who | Body |
| :--- | :--- | :--- | :--- |
| GET | `/api/notifications` | Any signed-in user |  |
| PUT | `/api/notifications/read-all` | Any signed-in user |  |
| PUT | `/api/notifications/:id/read` | Any signed-in user |  |

## `/api/billing`

| Method | Path | Who | Body |
| :--- | :--- | :--- | :--- |
| GET | `/api/billing/overview` | Admin |  |
| GET | `/api/billing/unit-balances` | Admin |  |
| GET | `/api/billing/runs` | Admin |  |
| POST | `/api/billing/runs/preview` (limited) | Admin | `period` (month), `maintenance_rate`? (number), `corpus_rate`? (number), `rate_basis`? (FLAT / PER_SQFT), `common_electricity_total`? (number), `common_water_total`? (number), `split_basis`? (EQUAL / PER_SQFT), `note`? (string) |
| POST | `/api/billing/runs` | Admin | `period` (month), `maintenance_rate`? (number), `corpus_rate`? (number), `rate_basis`? (FLAT / PER_SQFT), `common_electricity_total`? (number), `common_water_total`? (number), `split_basis`? (EQUAL / PER_SQFT), `due_date` (date), `note`? (string) |
| DELETE | `/api/billing/runs/:id` | Admin |  |
| GET | `/api/billing/invoices` | Admin |  |
| GET | `/api/billing/invoices/:id` | Admin |  |
| POST | `/api/billing/late-fees/preview` (limited) | Admin | `amount` (number), `basis`? (FLAT / PERCENT), `grace_days`? (integer), `max_amount`? (number), `period`? (month) |
| POST | `/api/billing/late-fees` | Admin | `amount` (number), `basis`? (FLAT / PERCENT), `grace_days`? (integer), `period`? (month) |
| GET | `/api/billing/invoices/:id/adjustments` | Admin |  |
| POST | `/api/billing/invoices/:id/adjustments` | Admin | `kind` (WAIVER / CREDIT / CORRECTION / LATE_FEE), `amount` (number), `reason` (string) |
| GET | `/api/billing/reminders` | Admin |  |
| POST | `/api/billing/reminders` | Admin | `period`? (month), `unit_id`? (integer) |
| GET | `/api/billing/receipts/:number` | Admin |  |
| GET | `/api/billing/declarations` | Admin |  |
| POST | `/api/billing/declarations/:id/review` | Admin | `approve` (boolean), `note`? (string) |
| POST | `/api/billing/invoices/:id/payments` | Admin | `amount` (number), `mode`? (UPI / CASH / BANK_TRANSFER / CHEQUE / OTHER), `reference`? (string), `paid_on`? (date) |

## `/api/resident`

| Method | Path | Who | Body |
| :--- | :--- | :--- | :--- |
| GET | `/api/resident/summary` | Resident |  |
| GET | `/api/resident/invoices` | Resident |  |
| GET | `/api/resident/declarations` | Resident |  |
| POST | `/api/resident/declarations` | Resident | `invoice_id` (integer), `amount` (number), `mode`? (UPI / CASH / BANK_TRANSFER / CHEQUE / OTHER), `reference`? (string), `paid_on`? (date) |
| GET | `/api/resident/documents` | Resident |  |
| GET | `/api/resident/receipts/:number` | Resident |  |
| GET | `/api/resident/tickets` | Resident |  |
| POST | `/api/resident/tickets` | Resident | `title` (string), `description` (string), `category`? (PLUMBING / ELECTRICAL / STRUCTURAL / LIFT / COMMON / OTHER), `scope`? (UNIT / COMMON), `location`? (string) |
| GET | `/api/resident/visitors` | Resident |  |
| GET | `/api/resident/passes` | Resident |  |
| POST | `/api/resident/passes` | Resident | `visitor_name` (string), `visitor_phone`? (string), `vehicle_number`? (string), `expected_on`? (date), `purpose`? (GUEST / DELIVERY / SERVICE / MAID / OTHER) |
| DELETE | `/api/resident/passes/:id` | Resident |  |

## `/api/helpers`

| Method | Path | Who | Body |
| :--- | :--- | :--- | :--- |
| GET | `/api/helpers/mine` | Resident |  |
| GET | `/api/helpers` | Admin, Security |  |
| GET | `/api/helpers/attendance` | Admin, Security |  |
| POST | `/api/helpers` | Admin | `name` (string), `helper_type` (string), `phone`? (string), `id_proof_type`? (string), `id_proof_number`? (string), `unit_ids` (list) |
| PUT | `/api/helpers/:id` | Admin | `name`? (string), `helper_type`? (string), `phone`? (string), `id_proof_type`? (string), `id_proof_number`? (string), `unit_ids`? (list), `is_active`? (boolean) |
| POST | `/api/helpers/:id/check-in` | Security |  |
| POST | `/api/helpers/:id/check-out` | Security |  |

## `/api/notices`

| Method | Path | Who | Body |
| :--- | :--- | :--- | :--- |
| GET | `/api/notices` | Any signed-in user |  |
| POST | `/api/notices/:id/acknowledge` | Any signed-in user |  |
| GET | `/api/notices/all` | Admin |  |
| POST | `/api/notices` | Admin | `title` (string), `body` (string), `category`? (GENERAL / MAINTENANCE / UTILITY / EVENT / URGENT), `audience`? (ALL / RESIDENT / SECURITY), `starts_on`? (date), `ends_on`? (date) |
| PUT | `/api/notices/:id` | Admin | `title`? (string), `body`? (string), `category`? (GENERAL / MAINTENANCE / UTILITY / EVENT / URGENT), `audience`? (ALL / RESIDENT / SECURITY), `starts_on`? (date), `ends_on`? (date), `is_published`? (boolean) |
| DELETE | `/api/notices/:id` | Admin |  |

## `/api/parking`

| Method | Path | Who | Body |
| :--- | :--- | :--- | :--- |
| GET | `/api/parking/bays` | Admin, Security |  |
| GET | `/api/parking/violations` | Admin, Security |  |
| POST | `/api/parking/bays` | Admin | `bay_number` (string), `level`? (string), `unit_id`? (integer), `vehicle_number`? (string), `notes`? (string) |
| PUT | `/api/parking/bays/:id` | Admin | `unit_id`? (integer), `vehicle_number`? (string), `level`? (string), `notes`? (string) |
| DELETE | `/api/parking/bays/:id` | Admin |  |
| POST | `/api/parking/violations` | Admin, Security | `bay_id` (integer), `vehicle_number` (string), `note`? (string) |
| PUT | `/api/parking/violations/:id` | Admin | `status` (string) |

## `/api/staff`

| Method | Path | Who | Body |
| :--- | :--- | :--- | :--- |
| GET | `/api/staff` | Admin |  |
| POST | `/api/staff` | Admin | `name` (string), `role_title` (string), `phone`? (string), `monthly_salary`? (number), `joined_on`? (date), `user_id`? (integer) |
| PUT | `/api/staff/:id` | Admin | `name`? (string), `role_title`? (string), `phone`? (string), `monthly_salary`? (number), `joined_on`? (date), `is_active`? (boolean) |
| GET | `/api/staff/attendance` | Admin |  |
| POST | `/api/staff/attendance` | Admin | `staff_id` (integer), `attendance_date` (date), `status` (string), `note`? (string) |

## `/api/audit`

| Method | Path | Who | Body |
| :--- | :--- | :--- | :--- |
| GET | `/api/audit` | Admin |  |
| GET | `/api/audit/:id` | Admin |  |

## `/api/finance`

| Method | Path | Who | Body |
| :--- | :--- | :--- | :--- |
| GET | `/api/finance/expenses` | Admin |  |
| POST | `/api/finance/expenses` | Admin | `category` (string), `amount` (number), `bill_date` (date), `paid_on`? (date), `payee_name`? (string), `reference`? (string), `asset_id`? (integer) |
| PUT | `/api/finance/expenses/:id` | Admin | `amount`? (number), `category`? (string), `fund`? (MAINTENANCE / CORPUS), `paid_on`? (date), `mode`? (UPI / CASH / BANK_TRANSFER / CHEQUE / OTHER), `reference`? (string), `note`? (string) |
| DELETE | `/api/finance/expenses/:id` | Admin | `reason` (string) |
| GET | `/api/finance/expenses/ticket/:ticket_id` | Admin |  |
| GET | `/api/finance/vendors` | Admin |  |
| POST | `/api/finance/vendors` | Admin | `name` (string), `service` (string), `phone`? (string) |
| PUT | `/api/finance/vendors/:id` | Admin | `name`? (string), `service`? (string), `contact_person`? (string), `phone`? (string), `email`? (string), `note`? (string), `is_active`? (boolean) |
| GET | `/api/finance/contracts` | Admin |  |
| GET | `/api/finance/contracts/expiring` | Admin |  |
| POST | `/api/finance/contracts` | Admin | `vendor_id` (integer), `title` (string), `start_date` (date), `end_date` (date), `amount`? (number) |
| PUT | `/api/finance/contracts/:id` | Admin | `end_date`? (date), `amount`? (number), `remind_days_before`? (integer), `note`? (string), `is_active`? (boolean) |
| GET | `/api/finance/statement` (limited) | Admin |  |
| GET | `/api/finance/statement/export` (limited) | Admin |  |
| GET | `/api/finance/close` | Admin |  |
| GET | `/api/finance/corpus` | Admin |  |
| GET | `/api/finance/budget` | Admin |  |
| POST | `/api/finance/budget` | Admin | `financial_year` (string), `category` (string), `amount` (number) |

## `/api/amenities`

| Method | Path | Who | Body |
| :--- | :--- | :--- | :--- |
| GET | `/api/amenities` | Any signed-in user |  |
| GET | `/api/amenities/:id/availability` | Any signed-in user |  |
| GET | `/api/amenities/bookings/all` | Any signed-in user |  |
| POST | `/api/amenities/bookings` | Resident | `amenity_id` (integer), `booking_date` (date), `starts_at` (string), `note`? (string) |
| DELETE | `/api/amenities/bookings/:id` | Admin, Resident |  |
| POST | `/api/amenities` | Admin | `name` (string), `slot_hours`? (integer), `charge`? (number) |
| PUT | `/api/amenities/:id` | Admin | `charge`? (number), `slot_hours`? (integer), `needs_approval`? (boolean), `is_active`? (boolean) |
| POST | `/api/amenities/bookings/:id/review` | Admin | `approve` (boolean), `note`? (string) |

## `/api/household`

| Method | Path | Who | Body |
| :--- | :--- | :--- | :--- |
| GET | `/api/household` | Resident |  |
| POST | `/api/household/members` | Resident | `name` (string), `relation`? (string), `phone`? (string) |
| DELETE | `/api/household/members/:id` | Resident |  |
| POST | `/api/household/vehicles` | Resident | `number_plate` (string), `vehicle_type`? (CAR / BIKE / SCOOTER / CYCLE / OTHER), `model`? (string) |
| DELETE | `/api/household/vehicles/:id` | Resident |  |
| PUT | `/api/household/directory` | Resident | `show_in_directory` (boolean) |
| GET | `/api/household/directory` | Resident |  |

## `/api/polls`

| Method | Path | Who | Body |
| :--- | :--- | :--- | :--- |
| GET | `/api/polls` | Admin, Resident |  |
| POST | `/api/polls/:id/vote` | Resident | `option_id` (integer) |
| POST | `/api/polls` | Admin | `question` (string), `opens_on` (date), `closes_on` (date) |
| PUT | `/api/polls/:id` | Admin | `close_now`? (boolean), `is_published`? (boolean) |
| GET | `/api/polls/:id/turnout` | Admin |  |

## `/api/parcels`

| Method | Path | Who | Body |
| :--- | :--- | :--- | :--- |
| GET | `/api/parcels` | Any signed-in user |  |
| GET | `/api/parcels/waiting` | Any signed-in user |  |
| POST | `/api/parcels` | Security | `unit_id` (integer), `courier`? (string), `description`? (string) |
| PUT | `/api/parcels/:id/release` | Security | `collected_by_name` (string) |

## `/api/emergency`

| Method | Path | Who | Body |
| :--- | :--- | :--- | :--- |
| GET | `/api/emergency/contacts` | Any signed-in user |  |
| POST | `/api/emergency/contacts` | Admin | `label` (string), `phone` (string) |
| DELETE | `/api/emergency/contacts/:id` | Admin |  |
| POST | `/api/emergency/alert` | Resident | `detail`? (string) |

## `/api/assets`

| Method | Path | Who | Body |
| :--- | :--- | :--- | :--- |
| GET | `/api/assets` | Admin |  |
| GET | `/api/assets/:id/history` | Admin |  |
| POST | `/api/assets` | Admin | `name` (string), `category`? (LIFT / PUMP / DG_SET / WATER_TANK / STP / FIRE_SAFETY / ELECTRICAL / OTHER), `location`? (string), `installed_on`? (date), `vendor_id`? (integer), `note`? (string) |
| PUT | `/api/assets/:id` | Admin | `name`? (string), `category`? (LIFT / PUMP / DG_SET / WATER_TANK / STP / FIRE_SAFETY / ELECTRICAL / OTHER), `location`? (string), `installed_on`? (date), `vendor_id`? (integer), `note`? (string), `is_active`? (boolean) |

## `/api/shifts`

| Method | Path | Who | Body |
| :--- | :--- | :--- | :--- |
| GET | `/api/shifts` | Admin, Security |  |
| GET | `/api/shifts/current` | Security |  |
| POST | `/api/shifts/start` | Security |  |
| POST | `/api/shifts/end` | Security | `handover_note`? (string) |
| POST | `/api/shifts/:id/acknowledge` | Security |  |

## `/api/exports`

| Method | Path | Who | Body |
| :--- | :--- | :--- | :--- |
| GET | `/api/exports/defaulters.csv` (limited) | Admin |  |
| GET | `/api/exports/gate.csv` (limited) | Admin |  |
| GET | `/api/exports/helper-attendance.csv` (limited) | Admin |  |
| GET | `/api/exports/sla-breaches.csv` (limited) | Admin |  |
| GET | `/api/exports/staff-pay.csv` (limited) | Admin |  |
| GET | `/api/exports/collections.csv` (limited) | Admin |  |

## Changes

- **Units.** `GET /api/units` returns the flat list of homes as `homeList`. Before
  September 2026 the same list was called `flatList`. The API is not versioned and
  has one client, the web app in this repository, which was changed in the same
  commit; any other client must read `homeList`.
